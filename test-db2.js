const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function main() { await prisma.university.create({ data: { name: 'Ozu', domain: 'ozu.edu.tr' } }); const users = await prisma.user.findMany({ select: { id: true, email: true, universityDomain: true } }); console.log('Users:', users); const unis = await prisma.university.findMany(); console.log('Universities:', unis); const count = await prisma.user.count({ where: { email: { endsWith: '@ozu.edu.tr' } } }); console.log('Count manual:', count); } main();  
